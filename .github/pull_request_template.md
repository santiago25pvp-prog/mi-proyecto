## Summary

- Describe what changed and why.

## Validation

- [ ] Backend typecheck (`npx tsc --noEmit`)
- [ ] Frontend typecheck (`cd frontend && npx tsc --noEmit`)
- [ ] Relevant tests

## Observability Override (Required when label `ops-override-observability` is used)

Leave empty if no override is needed.

Reason:
Risk:
Owner:
ExpiresAt:
RollbackPlan:
